# 03 音视频与媒体基础

> 状态：🟡 骨架 + 3.5 已填充 ｜ 难度：★★☆☆☆
> 本章要解决的问题：**画面到底是怎么从"摄像头"变成"另一台手机屏幕上的一帧"的？**

---

## 为什么需要这一章

前面 01 章已经知道媒体是 P2P 的 RTP 包。但"打包"之前，音视频原始数据经历了 采集 → 编码 → 封包；接收端反向 拆包 → 解码 → 渲染。这一章把中间的每一步讲透，理解后才能回答诸如：
- 为什么分辨率/帧率约束写在 `getUserMedia` 的 mandatory 里？
- 为什么 SDP 里会列出 H.264/VP8 一堆编码？
- 为什么偶发卡顿/马赛克？（丢包、无 FEC/NACK）
- 为什么相机端 CPU 发热？（编码很贵）

## 内容骨架（待填充章节）

### 3.1 数字音视频的基本概念（已列要点，待展开）
- [ ] 采样率 / 位深 / 声道（音频），帧 / 分辨率 / 帧率 / 像素格式 I420（视频）
- [ ] 码率 = 体积 / 时间，和画质的权衡
- [ ] 本项目的采集约束对应关系：`minWidth/minHeight/minFrameRate` → native 采集

### 3.2 编解码器（Codec）
- [ ] 为什么必须压缩（1 秒 1080p 未压缩 ≈ 上百 MB）
- [ ] 视频：H.264（硬件支持广，本项目主选）、VP8/VP9/AV1 对比；I/P/B 帧、关键帧
- [ ] 音频：Opus（本项目/WebRTC 默认）、G.711 等
- [ ] 软编 vs 硬编：对发热/耗电的影响
- [ ] 如何看两端最终协商用了什么编码 → 抓 SDP 的 m= 行

### 3.3 SDP：媒体能力说明书
- [ ] SDP 是文本，长什么样：`v= o= s= m= c= a=` 各段含义
- [ ] 关键行：`m=audio`、`m=video`、`a=rtpmap`、`a=sendrecv/sendonly/recvonly`、`a=mid`
- [ ] 媒体方向（sendrecv / sendonly / recvonly）与本项目：相机 sendonly？监控 recvonly？（注意 offer 里声明的是"我想收"）
- [ ] Unified-Plan：一条 m-line = 一个 transceiver（本项目 `sdpSemantics:'unified-plan'`）
- [ ] 去抓一次真实 SDP 保存为笔记样例（可在 flutter_webrtc 日志或加断点看 `description.sdp`）

### 3.4 RTP / RTCP：媒体怎么在网络上跑
- [ ] 媒体封装成 RTP 包（载荷类型 PT、SSRC、时间戳、序号）
- [ ] RTCP：丢包率/抖动统计反馈、接收者报告
- [ ] 抗丢包三板斧：NACK（重传）/ FEC（冗余）/ 码率自适应（拥塞控制）
- [ ] 本项目的通话质量为什么有时受 Wi-Fi 波动影响大

### 3.5 远端流渲染：从 `MediaStream` 句柄到屏幕上的一帧

> 状态：🟢 已填充
> 对应代码：`rephone-security/lib/pages/monitor_viewer_page.dart`、`rephone-security/lib/services/signaling.dart`；`flutter_webrtc` 原生侧
> 下文用 `$PUB` 代指 `~/.pub-cache/hosted/pub.flutter-io.cn/flutter_webrtc-1.6.0`。

**一句话结论：Dart 侧的 `MediaStream` 只是一个「查表用的句柄」，真正被渲染的是原生 libwebrtc 里的 `VideoTrack`；渲染器唯一的绑定点是 `videoTrack.addSink(renderer)`。**

链路总览：

```text
_monitor_viewer_page.dart:32   RTCVideoRenderer()        ← 纯 Dart 对象，还没有任何原生资源
        │
        ├─ :87  initialize() ──MethodChannel──► createVideoRenderer
        │        Android: textures.createSurfaceProducer() → producer.id() 即 textureId
        │                 + new FlutterRTCVideoRenderer(producer)
        │                 + new SurfaceTextureRenderer（extends EglRenderer）
        │        iOS:     [registry registerTexture:self] → textureId
        │      ◄──── {'textureId': 7} + 订阅 EventChannel('FlutterWebRTC/Texture7')
        │
        ├─ :686 RTCVideoView(_remoteRenderer) → Flutter 的 Texture(textureId: 7) 挂进 widget 树
        │
        ├─ :111 _remoteRenderer.srcObject = stream ──► videoRendererSetSrcObject(streamId, ownerTag)
        │        → 查表找到 MediaStream → videoTracks[0] → videoTrack.addSink(renderer)   ★ 唯一绑定点
        │
        └─ 帧到达：解码线程 → Renderer.onFrame() → 画进 producer 的 Surface → 引擎合成上屏
                          └─ 顺带上报「首帧 / 分辨率 / 旋转」→ Dart 侧 ValueNotifier → widget 自动重排
```

#### 3.5.1 先破除误解：Dart 的 `MediaStream` 里没有任何像素

```dart
// $PUB/lib/src/native/media_stream_impl.dart
class MediaStreamNative extends MediaStream {
  MediaStreamNative(super.streamId, super.ownerTag);

  factory MediaStreamNative.fromMap(Map<dynamic, dynamic> map) {
    return MediaStreamNative(map['streamId'], map['ownerTag'])
      ..setMediaTracks(map['audioTracks'], map['videoTracks']);
  }
```

它只持有三样东西：

| 字段 | 含义 | 作用 |
| --- | --- | --- |
| `streamId` | 流 ID | **查表主键** |
| `ownerTag` | 归属标记；远端流 = `peerConnectionId`，本地流 = `"local"` | **查哪张表的次键** |
| `_audioTracks` / `_videoTracks` | 轨道元数据（id / kind / label / enabled） | 只用来判断「有没有视频轨」，**不含像素** |

> 关键认知：**它是句柄（handle），不是数据容器**。像素始终留在原生 libwebrtc 的 `VideoTrack` 里，Dart 侧永远不经过。

#### 3.5.2 远端 `MediaStream` 是怎么诞生的（`srcObject` 之前）

```text
远端 SetRemoteDescription 成功
        │
        ▼ 原生（libwebrtc 线程）
PeerConnectionObserver.onAddTrack(receiver, streams)      ← Android，unified-plan
  ├─ params: event=onTrack, streams=[{streamId, ownerTag: id}], track, receiver
  └─ sendEvent() ──EventChannel──► Dart
        │
        └─ 同时把流登记进 PeerConnectionObserver.remoteStreams（onAddStream 时写入）
                │
                ▼ Dart
rtc_peerconnection_impl.dart  case 'onTrack'
  var streams = params.map((e) => MediaStreamNative.fromMap(e)).toList();
  onTrack?.call(RTCTrackEvent(streams: streams, track: ...))
                │
                ▼ 本项目信令层分流
signaling.dart:656  pc.onTrack = (event) {
    final stream = event.streams[0];
    if (event.track.kind == 'video')      onAddRemoteStream?.call(newSession, stream);
    else if (event.track.kind == 'audio') onAddRemoteAudioStream?.call(newSession, stream);
  }
                │
                ▼ 页面
monitor_viewer_page.dart:252  _syncRemoteStream(stream) → _remoteRenderer.srcObject = stream
```

**`ownerTag` 就是在 Dart 侧构造句柄时被写死的**：

```dart
// $PUB/lib/src/native/rtc_peerconnection_impl.dart  case 'onAddTrack'
var newStream = MediaStreamNative(streamId, _peerConnectionId);
var newStream = MediaStreamNative.fromMap(e);   // onTrack 路径：ownerTag 来自原生上报的 map
```

原生侧登记远端流的两个位置（注意平台不对称）：

| 平台 | 写入点 | 说明 |
| --- | --- | --- |
| iOS | `FlutterRTCPeerConnection.m` `didAddStream` **和** `didAddReceiver` | unified-plan 走 `didAddReceiver`，显式 `remoteStreams[mediaStreams[0].streamId] = mediaStreams[0]` |
| Android | `PeerConnectionObserver.onAddStream()`（`remoteStreams.put(streamId, mediaStream)`，第 405 行） | `onAddTrack()` 里**没有**写 `remoteStreams` |

> ⚠️ **待验证点**：本项目走 unified-plan，Android 侧 `remoteStreams` 只在 `onAddStream()` 写入。这条链能跑通，说明 libwebrtc 出于向后兼容仍为带 `a=msid` 的远端流触发了 `onAddStream`。验证方法：在 `onAddStream` / `onAddTrack` 各打一行日志，观察实际触发的是哪个。
> 这个细节解释了后面 3.5.3 的「`ownerTag` 传错 = 静默黑屏」。

#### 3.5.3 `srcObject = stream`：四次查表才落到 `VideoTrack`

```dart
// rephone-security/lib/pages/monitor_viewer_page.dart:104
void _syncRemoteStream(MediaStream stream) {
  _remoteStream = stream;
  if (stream.getVideoTracks().isNotEmpty) {
    _remoteRenderer.srcObject = stream;   // ← 用的是 setter
  }
}
```

```text
① Dart   setter → invokeMethod('videoRendererSetSrcObject',
                    {textureId, streamId: stream.id, ownerTag: stream.ownerTag})
                    // 注意：用 setter 时【不带 trackId】
② native renders.get(textureId)                     // 先按 textureId 找渲染器
③ native getStreamForId(streamId, ownerTag)         // 再按 (ownerTag, streamId) 找流
④ native stream.videoTracks.get(0)                  // 再取第一条视频轨
⑤ native videoTrack.addSink(surfaceTextureRenderer) // ★ 绑定
```

③ 是整个链路最关键的一次查表：

```java
// $PUB/android/.../MethodCallHandlerImpl.java:1649
MediaStream getStreamForId(String id, String peerConnectionId) {
  MediaStream stream = null;
  if (peerConnectionId.length() > 0) {
    PeerConnectionObserver pco = mPeerConnectionObservers.get(peerConnectionId);  // ← ownerTag 就是 key
    if (pco != null) {
      stream = pco.remoteStreams.get(id);
    }
  } else {
    for (Entry<String, PeerConnectionObserver> entry : mPeerConnectionObservers.entrySet()) {
      // 退化路径：遍历所有 PeerConnection 找同名的流
    }
  }
  if (stream == null) {
    stream = localStreams.get(id);   // 再退一步找本地流
  }
  return stream;
}
```

调用者处的分支决定了「取哪条轨」：

```java
// $PUB/android/.../MethodCallHandlerImpl.java:728
if (ownerTag.equals("local")) {
  stream = localStreams.get(streamId);
} else {
  stream = getStreamForId(streamId, ownerTag);
}
if (trackId != null && !trackId.equals("0")) {
  MediaStreamTrack track = getTrackForId(trackId, ownerTag);
  if (track instanceof VideoTrack) render.setTrack((VideoTrack) track, streamId, ownerTag);
  else                             render.setStream(stream, trackId, ownerTag);
} else {
  render.setStream(stream, ownerTag);   // ← 本项目走这里：只取 videoTracks[0]
}
```

| 调用方式 | 参数 | 选中哪条轨 |
| --- | --- | --- |
| `renderer.srcObject = stream`（setter，本项目） | 3 个 | `videoTracks.get(0)`，**无法指定第 2 条** |
| `renderer.setSrcObject(stream: s, trackId: t)`（方法） | 4 个 | 按 `trackId` 精确匹配 |

> 如果将来相机端一条流里带多路视频（如主流 + 屏幕共享），必须从 setter 换成 `setSrcObject(trackId:)`。

#### 3.5.4 `addSink`：唯一的绑定落点

```java
// $PUB/android/.../FlutterRTCVideoRenderer.java:249
private void tryAddRendererToVideoTrack() throws Exception {
  if (videoTrack != null) {
    EglBase.Context sharedContext = EglUtils.getRootEglBaseContext();
    if (sharedContext == null) { /* 直接放弃渲染，避免 SurfaceViewRenderer.init 抛异常杀进程 */ return; }

    surfaceTextureRenderer.release();
    listenRendererEvents();
    surfaceTextureRenderer.init(sharedContext, rendererEvents);
    surfaceTextureRenderer.surfaceCreated(producer);

    videoTrack.addSink(surfaceTextureRenderer);   // ★ 渲染真正开始的地方
  }
}
```

- `addSink` 是 libwebrtc 的标准观察者接口。**从这一行起，解码线程每产出一帧就回调 `SurfaceTextureRenderer.onFrame()`**。
- 每次都先 `release()` 再 `init()`：换轨（simulcast 层切换 / 重新绑定）时必须重建 GL 状态与 `rendererEvents`。
- `EglUtils.getRootEglBaseContext()` 是**全局共享的 EGL 上下文**——全 App 共一套 GL 纹理与 shader，这也是为什么必须判空（共享上下文没初始化好，渲染器无从创建）。
- 反向解绑在 `setVideoTrack(null)` 分支 → `videoTrack.removeSink(surfaceTextureRenderer)`。

#### 3.5.5 一帧的生命周期：Android 与 iOS 走了两条完全不同的路

| | Android | iOS |
| --- | --- | --- |
| 帧入口 | `SurfaceTextureRenderer.onFrame(VideoFrame)` | `FlutterRTCVideoRenderer.renderFrame:` |
| 传递方式 | **GPU 共享**：EGL 直接画到纹理背后的 Surface | **CPU 拷贝**：I420 → `CVPixelBuffer` |
| 旋转 | libwebrtc / GPU 完成 | 手写 `RTCYUVHelper I420Rotate` |
| 通知引擎 | 提交 EGLSurface（引擎自行合成） | `[_registry textureFrameAvailable:_textureId]` |
| 引擎取数 | 无需回调 | 引擎主动调 `copyPixelBuffer` |
| 开销 | 低（无拷贝） | 每帧一次 memcpy + 旋转 |

Android：**Surface 在第一帧到达时才创建**，`producer.setSize()` 用帧的旋转后分辨率：

```java
// $PUB/android/.../SurfaceTextureRenderer.java:100
public void onFrame(VideoFrame frame) {
  synchronized (surfaceLock) {
    if (surface == null) {
      producer.setSize(frame.getRotatedWidth(), frame.getRotatedHeight());
      surface = producer.getSurface();
      createEglSurface(surface);
    } else if (frameSizeChanged(frame)) {
      // The producer's backing buffers are fixed-size: setSize() only takes
      // effect for a Surface obtained afterwards. Without recreating the EGL
      // surface here, a simulcast layer upgrade keeps rendering into the old
      // low-resolution buffer and the video stays blurry.
      releaseEglSurface(() -> {});
      surface = null;                                   // 先置空，避免 getSurface 抛异常后画进已释放的 Surface
      producer.setSize(frame.getRotatedWidth(), frame.getRotatedHeight());
      surface = producer.getSurface();
      createEglSurface(surface);
    }
  }
  updateFrameDimensionsAndReportEvents(frame);
  super.onFrame(frame);   // EglRenderer：投递到自己的渲染线程，用 GlRectDrawer 把帧画到 EGLSurface
}
```

> **坑位**：`SurfaceProducer` 的底层 buffer 是**定长**的，`setSize()` 只对「之后获取的 Surface」生效。所以在 `else if` 分支里**必须 release EGLSurface → setSize → 重新 getSurface → createEglSurface**。否则 simulcast 从低清层升到高清层时，画面会一直停留在旧的低分辨率上（模糊），而分辨率事件却已经上报给 Dart 了（表现为「布局变对了，画面还是糊的」）。

iOS：多了一个节流标志，防止解码快于合成：

```objc
// $PUB/ios/.../FlutterRTCVideoRenderer.m:194  renderFrame:
if(!_frameAvailable && _pixelBufferRef) {
  [self copyI420ToCVPixelBuffer:_pixelBufferRef withFrame:frame];
  if(_textureId != -1) { [_registry textureFrameAvailable:_textureId]; }
  _frameAvailable = true;
}

// $PUB/ios/.../FlutterRTCVideoRenderer.m:54  copyPixelBuffer（由引擎回调）
if (_pixelBufferRef != nil && _frameAvailable) {
  buffer = CVBufferRetain(_pixelBufferRef);
  _frameAvailable = false;   // 引擎取走后解锁，下一帧才能写入
}
```

#### 3.5.6 事件回流：尺寸 / 旋转 / 首帧如何让 widget 自动重排

```text
原生 RendererEvents.onFrameResolutionChanged / onFirstFrameRendered
        │  eventSink.success({event: didTextureChangeVideoSize, width, height, rotation})
        ▼  EventChannel('FlutterWebRTC/Texture$textureId')
Dart   eventListener: value = value.copyWith(width:, height:, rotation:, renderVideo:)
        │  RTCVideoRenderer extends ValueNotifier<RTCVideoValue> → 自动 notifyListeners
        ▼
RTCVideoView 的 ValueListenableBuilder rebuild
  → SizedBox(width: maxHeight * value.aspectRatio, height: maxHeight)
  → FittedBox(fit: BoxFit.contain / cover)
```

`aspectRatio` 对旋转 90°/270° 会**交换宽高**：

```dart
// $PUB/../webrtc_interface-1.5.1/lib/src/rtc_video_renderer.dart
double get aspectRatio {
  if (width == 0.0 || height == 0.0) return 1.0;
  return (rotation == 90 || rotation == 270) ? height / width : width / height;
}
```

> **这条链上没有任何业务 `setState`**。「横竖屏翻转时画面容器自动跟着变」完全由 `ValueNotifier` 驱动。

#### 3.5.7 渲染层实践要点与坑位

| 点 | 说明 |
| --- | --- |
| 为什么要赋两次 `srcObject` | unified-plan 下 audio/video 的 `onTrack` 顺序不定；有视频轨时才绑主渲染器，音频轨后到再赋一次以刷新部分机型的播放（`monitor_viewer_page.dart:102-103` 注释） |
| 挂断时置 `null` | `:217` / `:272` → 发 `streamId: ''` → 原生查到 `null` → `setVideoTrack(null)` → `removeSink`。**纹理本身保留**，下次通话可复用 |
| `dispose()` 必须调用 | `:82`。原生侧 `producer.release()` + `surfaceTextureRenderer.release()` + `eventChannel.setStreamHandler(null)`；漏掉 = Flutter 纹理与 EGL surface 双重泄漏，反复进出页面会崩 |
| `ownerTag` 传错 = 静默黑屏 | `getStreamForId` 查不到时静默返回 `null`，**无异常、无日志**。这是最难排查的一类渲染问题 |
| `initialize()` 幂等 | Dart 侧用 `Completer _initializing` 保护，重复调用只 await 同一个 future（本项目在 `initState` 里 fire-and-forget 调用） |
| `renderVideo` 是门闸 | `_textureId != null && _srcObject != null` 才画 `Texture`，否则显示占位符——所以「已 initialize 但没设流」不会黑屏而是空白 |
| 一套纹理一个渲染器 | textureId 全局唯一、不跨页面共享；同页两个 `RTCVideoRenderer` = 两块 GPU 合成层 |

#### 3.5.8 回声与音频路由（待展开）

- [ ] 回声消除 AEC、降噪、自动增益（native 默认开）——结合 3.5.5 的「iOS 每帧 CPU 拷贝」理解为什么 iOS 端功耗更高
- [ ] 本项目 iOS 通话音频路由切换代码为什么必须在 `invite` 前调用（`monitor_viewer_page.dart` 的 `_applyIosMonitorAudioMode`）
- [ ] 对讲（monitor talkback）时本地麦克风与远端播放的相互干扰

### 3.6 本项目"录下来的是什么"
- [ ] 相机端前台录制：采集→编码→落盘 vs 本端到端直播的关系
- [ ] 回放页为什么用 DataChannel 传文件而不是 RTMP/HTTP？（端上文件、P2P 直传）

## 待填充清单（每次补一节就更新）
- [ ] 3.1 数字音视频基础
- [ ] 3.2 编解码器
- [ ] 3.3 SDP 详解 + 真实 SDP 样例
- [ ] 3.4 RTP/RTCP 与抗丢包
- [x] 3.5 渲染（远端流渲染全链路已填充；回声/音频路由见 3.5.8 待展开）
- [ ] 3.6 录制链路

## 对应本项目（占位）
- 采集约束：`signaling.dart` `createStream()`（已见 01 章）
- 编码协商：SDP 里见分晓 → `_createOffer/_createAnswer` 的 `description.sdp`
- 远端渲染：`monitor_viewer_page.dart` `_remoteRenderer`（`initialize()` → `srcObject` → `RTCVideoView`），详见 3.5
- 音频路由：`monitor_viewer_page.dart` `_applyIosMonitorAudioMode()`
